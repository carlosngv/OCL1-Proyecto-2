

class Parser {
    constructor(tokenList) {
        var numPreAnalysis;
        var preAnalysis
        var tokenList;
    }

    parse(tokenList) {
        this.preAnalysis = '';
        this.numPreAnalysis = 0;
        this.tokenList = tokenList;
        this.start();
    }
    
    start() {
        
    }
}